{
  "name": "@bt/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "migrate:local": "wrangler d1 migrations apply backtest --local",
    "migrate:remote": "wrangler d1 migrations apply backtest --remote"
  },
  "dependencies": {
    "@bt/data": "workspace:*",
    "@bt/engine": "workspace:*",
    "@bt/schemas": "workspace:*",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251101.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.95.0"
  }
}
