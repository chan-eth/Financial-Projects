# Public — embedded in the bundle, used by the client to know where to call.
NEXT_PUBLIC_API_BASE=https://api.getdebanked.xyz

# Server-only — used by app/api/proxy/[...path]/route.ts to call the Worker.
WORKER_INTERNAL_SECRET=replace-me
