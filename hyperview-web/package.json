{
  "name": "hyperview-web",
  "version": "0.0.0",
  "private": true,
  "description": "HyperView web app (Next.js). Scaffold-only at M0; see ../hyperview/PLAN.md.",
  "scripts": {
    "dev": "echo 'M0 scaffold — Next.js install pending' && exit 0",
    "build": "echo 'M0 scaffold — Next.js install pending' && exit 0",
    "typecheck": "echo 'M0 scaffold — typecheck pending' && exit 0",
    "lint": "echo 'M0 scaffold — lint pending' && exit 0"
  }
}
